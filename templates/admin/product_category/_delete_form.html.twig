<form method="post" action="{{ path('app_admin_product_category_delete', {'id': product_category.id}) }}" onsubmit="return confirm('Czy jesteś pewny usunięcia elementu?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product_category.id) }}">
    <button class="btn btn-danger">Usuń</button>
</form>
