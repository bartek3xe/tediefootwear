{% extends 'admin/base.html.twig' %}

{% block title %}Kategoria {{ product_category.getNameByLanguage('pl')}}{% endblock %}

{% block body %}
    <h1>Kategoria {{ product_category.getNameByLanguage('pl') }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ product_category.getNameByLanguage('pl') }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ product_category.slug }}</td>
            </tr>
        </tbody>
    </table>

    <div class="d-flex gap-2">
        <a class="btn btn-dark" href="{{ path('app_admin_product_category_index') }}">Powrót do listy</a>
        <a class="btn btn-warning" href="{{ path('app_admin_product_category_edit', {'id': product_category.id}) }}">Edytuj</a>
        {{ include('admin/product_category/_delete_form.html.twig') }}
    </div>
{% endblock %}
